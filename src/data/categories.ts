import { Category } from '../types/Category'

export const categories: Category = {
    expense: { title: 'Despesa', color: 'red', expense: true },
    receive: {title: 'Receita', color: 'green', expense: false}
}